import {
  conditional
} from '../../../helpers/conditional';
import { module, test } from 'qunit';

module('ConditionalHelper');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = conditional(42);
  assert.ok(result);
});
